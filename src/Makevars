UNAME := $(shell uname)

ifeq ($(UNAME), Darwin)
# macOS (Darwin) settings for Clang
PKG_CXXFLAGS = -Xpreprocessor -fopenmp
PKG_LIBS = -lomp
else
# Linux settings for GCC
PKG_CXXFLAGS = -fopenmp
PKG_LIBS = -fopenmp
endif
